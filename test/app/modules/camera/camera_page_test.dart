import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/modules/camera/camera_page.dart';

main() {
  testWidgets('CameraPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CameraPage(title: 'Camera')));
    //  final titleFinder = find.text('Camera');
    //  expect(titleFinder, findsOneWidget);
  });
}
