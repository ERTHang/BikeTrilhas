import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:biketrilhas_modular/app/modules/camera/camera_controller.dart';
import 'package:biketrilhas_modular/app/modules/camera/camera_module.dart';

void main() {
  initModule(CameraModule());
  // CameraController camera;
  //
  setUp(() {
    //     camera = CameraModule.to.get<CameraController>();
  });

  group('CameraController Test', () {
    //   test("First Test", () {
    //     expect(camera, isInstanceOf<CameraController>());
    //   });

    //   test("Set Value", () {
    //     expect(camera.value, equals(0));
    //     camera.increment();
    //     expect(camera.value, equals(1));
    //   });
  });
}
