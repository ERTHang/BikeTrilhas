import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/modules/photo/photo_page.dart';

main() {
  testWidgets('PhotoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PhotoPage(title: 'Photo')));
    //  final titleFinder = find.text('Photo');
    //  expect(titleFinder, findsOneWidget);
  });
}
