import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/modules/map/map_page.dart';

main() {
  testWidgets('MapPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MapPage(title: 'Map')));
    //  final titleFinder = find.text('Map');
    //  expect(titleFinder, findsOneWidget);
  });
}
