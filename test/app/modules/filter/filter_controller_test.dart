import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:biketrilhas_modular/app/modules/filter/filter_controller.dart';
import 'package:biketrilhas_modular/app/modules/filter/filter_module.dart';

void main() {
  initModule(FilterModule());
  // FilterController filter;
  //
  setUp(() {
    //     filter = FilterModule.to.get<FilterController>();
  });

  group('FilterController Test', () {
    //   test("First Test", () {
    //     expect(filter, isInstanceOf<FilterController>());
    //   });

    //   test("Set Value", () {
    //     expect(filter.value, equals(0));
    //     filter.increment();
    //     expect(filter.value, equals(1));
    //   });
  });
}
