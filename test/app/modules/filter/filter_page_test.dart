import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/modules/filter/filter_page.dart';

main() {
  testWidgets('FilterPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FilterPage(title: 'Filter')));
    //  final titleFinder = find.text('Filter');
    //  expect(titleFinder, findsOneWidget);
  });
}
