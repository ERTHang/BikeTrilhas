import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/modules/filter/utils/map_filter/map_filter_page.dart';

main() {
  testWidgets('MapFilterPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MapFilterPage(title: 'MapFilter')));
    //  final titleFinder = find.text('MapFilter');
    //  expect(titleFinder, findsOneWidget);
  });
}
