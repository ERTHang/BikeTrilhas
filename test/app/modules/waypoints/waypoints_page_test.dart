import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/modules/waypoints/waypoints_page.dart';

main() {
  testWidgets('WaypointsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WaypointsPage(title: 'Waypoints')));
    //  final titleFinder = find.text('Waypoints');
    //  expect(titleFinder, findsOneWidget);
  });
}
