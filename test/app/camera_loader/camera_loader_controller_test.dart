import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:biketrilhas_modular/app/camera_loader/camera_loader_controller.dart';
import 'package:biketrilhas_modular/app/app_module.dart';

void main() {
  initModule(AppModule());
  // CameraLoaderController cameraloader;
  //
  setUp(() {
    //     cameraloader = AppModule.to.get<CameraLoaderController>();
  });

  group('CameraLoaderController Test', () {
    //   test("First Test", () {
    //     expect(cameraloader, isInstanceOf<CameraLoaderController>());
    //   });

    //   test("Set Value", () {
    //     expect(cameraloader.value, equals(0));
    //     cameraloader.increment();
    //     expect(cameraloader.value, equals(1));
    //   });
  });
}
