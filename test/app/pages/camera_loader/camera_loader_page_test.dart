import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/pages/camera_loader/camera_loader_page.dart';

main() {
  testWidgets('CameraLoaderPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CameraLoaderPage(title: 'CameraLoader')));
    //  final titleFinder = find.text('CameraLoader');
    //  expect(titleFinder, findsOneWidget);
  });
}
