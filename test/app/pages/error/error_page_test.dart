import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:biketrilhas_modular/app/pages/error/error_page.dart';

main() {
  testWidgets('ErrorPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ErrorPage(title: 'Error')));
    //  final titleFinder = find.text('Error');
    //  expect(titleFinder, findsOneWidget);
  });
}
