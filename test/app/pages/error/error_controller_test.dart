import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:biketrilhas_modular/app/pages/error/error_controller.dart';
import 'package:biketrilhas_modular/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ErrorController error;
  //
  setUp(() {
    //     error = AppModule.to.get<ErrorController>();
  });

  group('ErrorController Test', () {
    //   test("First Test", () {
    //     expect(error, isInstanceOf<ErrorController>());
    //   });

    //   test("Set Value", () {
    //     expect(error.value, equals(0));
    //     error.increment();
    //     expect(error.value, equals(1));
    //   });
  });
}
